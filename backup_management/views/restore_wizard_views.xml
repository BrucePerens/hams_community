<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_backup_restore_wizard_form" model="ir.ui.view"> <!-- audit-ignore-view: Tested by [@ANCHOR: test_backup_view] -->
        <field name="name">backup.restore.wizard.form</field>
        <field name="model">backup.restore.wizard</field>
        <field name="arch" type="xml">
            <form string="Restore Snapshot">
                <group>
                    <field name="snapshot_id" readonly="1"/>
                    <field name="restore_target_path"/>
                </group>
                <footer>
                    <button string="Restore" name="action_restore" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_backup_restore_wizard" model="ir.actions.act_window">
        <field name="name">Restore Snapshot</field>
        <field name="res_model">backup.restore.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
